<div class="sidebar" id="sidebar">
    <div class="menu-item">
        <a href="#">Главная</a>
    </div>
    <div class="menu-item">
        <a href="#">Мои задачи</a>
    </div>
    <div class="menu-item">
        <a href="#">Входящие</a>
    </div>
    <div class="menu-item">
        <div class="dropdown">
            <button class="dropbtn create-btn" style="margin: 0 auto; width: 150px; height: 30px;"
                    onclick="toggleCreateDropdown()">Создать
            </button>

            <div class="create-dropdown-content" id="create-dropdown-content">
                <a href="#">Задача</a>
                <a href="#">Проект</a>
                <a href="#">Сообщение</a>
            </div>
        </div>
    </div>
</div>


                <form action="{% url 'orders:main' %}" method="POST">
                    {% csrf_token %}
                    <p>
                        <label for="{{ task.name.id_for_label }}">Введите название</label><br>
                        <input type="text" name="name" value="{{ task.name }}"/>
                    </p>
                    <p>
                        <label>Дата создания</label><br>
                        <input id="one" type="date" name="date" value="{{ task.date }}"/>
                    </p>
                    <p>
                        <label>Введите дату дедлайна</label><br>
                        <input type="date" name="expired" value="{{ task.expired }}"/>
                    </p>
                    <p>
                        <label>Статус задачи</label><br>
                        <select name="status">
                            <option value="{{ task.status }}">Не завершено</option>
                            <option value="{{ task.status }}">В процесса</option>
                            <option value="{{ task.status }}">Готово</option>
                        </select>
                    </p>
                    <p>
                        <label>Описание</label><br>
                        <input type="text" name="description" value="{{ task.description }}"/>
                    </p>
                    <p>
                        <label>Автор</label><br>
                        <input type="text" name="initiator" value="admin" disabled/>
                    </p>
                    <input type="submit" value="Сохранить">
                </form>